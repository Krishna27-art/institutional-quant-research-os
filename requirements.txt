# ============================================================
# Quant Research OS — Requirements
# Updated: 2026-06-02
# ============================================================

# Core Data Processing
pandas>=2.0.0
numpy>=1.24.0
polars>=0.19.0
pyarrow>=12.0.0

# Data Sources
yfinance>=0.2.0
nselib>=0.2.0
kiteconnect>=4.0.0

# Statistical & Classical ML
scipy>=1.10.0
statsmodels>=0.14.0
scikit-learn>=1.3.0

# ── Tabular Boosting Models (Priority: HIGH per audit) ──────
lightgbm>=4.0.0
xgboost>=2.0.0
catboost>=1.2.0
shap>=0.42.0

# ── Hyperparameter Optimization ──────────────────────────────
optuna>=3.4.0

# ── HMM & Regime Detection (hmmlearn was missing → crash) ───
hmmlearn>=0.2.8
ruptures>=1.1.7          # Bayesian change-point detection

# ── HuggingFace Ecosystem ────────────────────────────────────
# Required for FinBERT, PatchTST, FinGPT
transformers>=4.40.0
accelerate>=0.27.0
sentencepiece>=0.1.99    # FinGPT tokenizer
einops>=0.7.0            # PatchTST patch operations
datasets>=2.18.0         # HuggingFace datasets utility
tokenizers>=0.19.0

# ── Time-Series Foundation Models ────────────────────────────
# Amazon Chronos (install from source for latest)
# pip install git+https://github.com/amazon-science/chronos-forecasting.git
# Listed separately in install script; chronos-forecasting not on PyPI stable

# Google TimesFM
timesfm>=1.2.6

# NeuralForecast stack (PatchTST, N-BEATS, TFT via Nixtla)
neuralforecast>=1.7.0
utilsforecast>=0.1.0

# TimeGPT via Nixtla API (optional — requires NIXTLA_API_KEY)
nixtla>=0.6.0

# ── Deep Learning (for NLP & Foundation Models only) ─────────
torch>=2.0.0
# torch-geometric removed — GCN not used in production (per audit)

# ── Optimization ─────────────────────────────────────────────
cvxpy>=1.4.0

# ── Database & Caching ───────────────────────────────────────
redis>=4.5.0
clickhouse-connect>=0.6.0
psycopg2-binary>=2.9.0
sqlalchemy>=2.0.0
arctic>=1.80.0
pymongo>=4.0.0

# ── API & Streaming ──────────────────────────────────────────
fastapi>=0.100.0
uvicorn>=0.23.0
websockets>=11.0.0
httpx>=0.24.0

# ── Performance ──────────────────────────────────────────────
numba>=0.57.0

# ── Visualization ────────────────────────────────────────────
matplotlib>=3.7.0
plotly>=5.15.0

# ── Utilities ────────────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.0.0
typer>=0.9.0
pyyaml>=6.0
tqdm>=4.65.0             # Progress bars for model downloads

# ── Monitoring ───────────────────────────────────────────────
prometheus-client>=0.17.0

# ── C++ Build Dependencies ───────────────────────────────────
pybind11>=2.11.0
cmake>=3.25.0
